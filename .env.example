# Frontend environment variables (Vite)
# Base URL of the backend API (used in development mode only;
# in production the API is served from the same domain under /api)
VITE_API_BASE_URL=http://localhost:5000
